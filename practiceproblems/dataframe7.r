# Create a data frame with columns for name, age, and city for 3 people, then print the data frame


df <- data.frame(
 name = c("sneha", "mriti", "krish"),
 age = c(25, 30, 35),
 city = c("kol", "how", "mur")
)
print(df)